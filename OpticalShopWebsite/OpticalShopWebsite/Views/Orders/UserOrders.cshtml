@model List<OpticalShopWebsite.Models.Order>

@{
    ViewData["Title"] = "My Orders";
}

<div class="container my-5">
    <h2 class="text-center mb-4">My Orders</h2>

    @if (Model.Count == 0)
    {
        <div class="alert alert-info text-center" role="alert">
            You have not placed any orders yet.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Product</th>
                        <th>Order Date</th>
                        <th>Amount</th>
                        <th>Payment Status</th>
                        <th>Order Status</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var order in Model)
                    {
                        var product = ViewData[$"Product_{order.Id}"] as OpticalShopWebsite.Models.Product;
                        <tr>
                            <td>@order.Id</td>
                            <td>@(product != null ? product.Name : "Unknown Product")</td>
                            <td>@order.OrderDate.ToString("dd MMM yyyy")</td>
                            <td>₹@order.Amount</td>
                            <td>@order.PaymentStatus</td>
                            <td>@order.OrderStatus</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>





@* @model List<OpticalShopWebsite.Models.Order>

@{
    ViewData["Title"] = "My Orders";
}

<h2>My Orders</h2>

@if (Model.Count == 0)
{
    <p>You have not placed any orders yet.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Order ID</th>
                <th>Product</th>
                <th>Order Date</th>
                <th>Amount</th>
                <th>Payment Status</th>
                <th>Order Status</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                var product = ViewData[$"Product_{order.Id}"] as OpticalShopWebsite.Models.Product;
                <tr>
                    <td>@order.Id</td>
                    <td>@(product != null ? product.Name : "Unknown Product")</td>
                    <td>@order.OrderDate.ToString("dd MMM yyyy")</td>
                    <td>₹@order.Amount</td>
                    <td>@order.PaymentStatus</td>
                    <td>@order.OrderStatus</td>
                </tr>
            }
        </tbody>
    </table>
}
 *@